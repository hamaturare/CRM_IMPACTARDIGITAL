from django.db import models
from django.utils.translation import gettext_lazy as _
from django.conf import settings

# Create your models here.

class WpMessages(models.Model):
    #created_at = models.DateField(auto_now_add=True, verbose_name=_("Data da Menssagem"))
    #name = models.CharField(max_length=20, verbose_name=_("Nome da Lead"), blank=True)
    #phone_number = models.CharField(max_length=20, verbose_name=_("Whatsapp (Telefone)"), blank=True)
    #message = models.TextField(max_length=250, blank=True, verbose_name=_("Mensagem"))
    pass


